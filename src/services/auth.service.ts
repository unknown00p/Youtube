import { User } from "../models/user.models";
import type { authType } from "../types/auth.types";

function signup_service({ username, email, password }: authType) {
  // rest of the logic goes here
}